## 1. Controller: 200 re-render + flash/PRG

- [x] 1.1 In `app/actions/verwaltung/offering-configs/controller.tsx`, remove every non-OK status override from the create and update validation re-renders (`{ status: validation.status }`, `{ status: 400 }`, `{ status: 404 }`, `{ status: 409 }`) so the fragment renders at status 200 with `fieldErrors`/`formError` and preserved `formValues` (schema issues, invalid resource, resource not found, duplicate resource, no day enabled, invalid time range). Verify: `npm test -- "**/offering-configs.test.ts"` passes and `npm run typecheck` passes.
- [x] 1.2 In update and destroy, replace the `context.json({ ok: false, error })` returns (invalid id, not found) with a redirect to the grid URL (grid state preserved via `gridStateFromFormData` → `gridStateToParams`) plus `context.session.flash('error', message)`. Verify: `grep -n "context.json" app/actions/verwaltung/offering-configs/controller.tsx` returns no human-flow mutation error path (only the agent `X-Agent-Thread` branch stays); `npm test -- "**/offering-configs.test.ts"` passes.
- [x] 1.3 In destroy, replace the constraint-violation re-render (`{ status: 400 }`) with a redirect to the grid URL (grid state preserved) plus `context.session.flash('error', message)`. Verify: the constraint-violation test asserts a 3xx redirect with a flash instead of a non-OK body.
- [x] 1.4 In the `show` action, when `getOfferingConfig(...)` returns null, redirect (3xx) to the grid index (grid state from the URL preserved) instead of rendering `{ status: 404 }`; keep rendering the edit panel at 200 when the row exists. Verify: a `GET /verwaltung/offering-configs/:id` for a missing/deleted row returns a 302 to the grid.
- [x] 1.5 Confirm the agent (`X-Agent-Thread`) create branch is unchanged (still returns `context.json` `validation_error`/`created` and the 409 constraint path). Verify: `grep -n "X-Agent-Thread" app/actions/verwaltung/offering-configs/controller.tsx` still shows the JSON branch; the agent-mode tests pass.

## 2. Page: visible server-rendered row actions

- [x] 2.1 In `app/ui/admin-offering-configs-page.tsx`, add a per-row action cell using `table.actionCell` with a server-rendered edit anchor (`href` = `buildEditUrl(ADMIN_BASE, row.id, offset, sortColumn, sortDirection, filter)`) using `data-rmx-target={frames.adminContent}`. Verify: a rendered offering-configs fragment contains a per-row `a[data-rmx-target="admin-content"]` edit link; `npm run typecheck` passes.
- [x] 2.2 Add a per-row DELETE `RestfulForm` (method DELETE, action = `routes.verwaltung.offeringConfigs.destroy`) carrying `data-delete-form={row.id}`, `data-confirm`, `data-rmx-target={frames.adminContent}`, and `GridStateHiddenInputs` (offset/sort/order/filter). Verify: the fragment contains `form[data-delete-form]` with `data-rmx-target="admin-content"`; `npm run typecheck` passes.
- [x] 2.3 Remove the existing hidden bulk DELETE forms block (the visible per-row form now carries `data-delete-form`). Verify: `grep -n "data-delete-form" app/ui/admin-offering-configs-page.tsx` shows one DELETE form per row and no hidden bulk block; `npm run typecheck` passes.
- [x] 2.4 Add an empty-state `Neu anlegen` frame-targeted CTA inside the empty-table branch (rendered only when no form panel is active), mirroring the resources page. Verify: a fragment rendered with no rows and no form panel contains the empty-state CTA targeting the content frame; `npm run typecheck` passes.
- [x] 2.5 Ensure the grid-state JSON blob (`#offering-configs-grid-state`) carries the fields the context-menu edit needs (offset/sort/order/filter/baseHref). Verify: `npm run typecheck` passes; the blob already includes these fields.

## 3. Context menu: input affordance only

- [x] 3.1 In `app/actions/admin/public/admin-offering-configs-context-menu.tsx`, replace the edit action's `window.location.href` with frame-aware `safeNavigate(baseHref + '?' + params, handle)` (import from `utils/frame-utils.ts`) while preserving offset/sort/order/filter. Verify: `grep -rn "window.location.href" app/actions/admin/public/admin-offering-configs-context-menu.tsx` returns nothing; `npm run typecheck` passes.
- [x] 3.2 Keep the delete menu item submitting the row's `form[data-delete-form]` via `form.requestSubmit()` (guarded for null), now targeting the visible per-row form. Verify: `grep -rn "requestSubmit" app/actions/admin/public/admin-offering-configs-context-menu.tsx` shows the delete path and no `fetch(` mutation call.

## 4. Tests and verification

- [x] 4.1 Update `app/actions/verwaltung/offering-configs.test.ts` to assert status 200 inline-error re-renders (not 400/404) for create and update validation failures (duplicate resource, non-existent resource, invalid time range, missing resource, out-of-range times). Verify: `npm test -- "**/offering-configs.test.ts"` passes.
- [x] 4.2 Update the destroy and update tests to assert a 3xx redirect plus a flash for invalid id / not-found / constraint-violation (not `response.json()` 404 bodies). Verify: `npm test -- "**/offering-configs.test.ts"` passes.
- [x] 4.3 Add/adjust a test asserting `GET /verwaltung/offering-configs/:id` for a missing/deleted row redirects to the grid (no 404 card), and that the agent-mode tests still pass. Verify: `npm test -- "**/offering-configs.test.ts"` passes.
- [x] 4.4 Confirm no client-side mutation path remains: `grep -rn "frame.reload\|fetch(" app/actions/verwaltung/offering-configs app/actions/admin/public/admin-offering-configs-context-menu.tsx` returns no mutation call beyond the confirm/context affordances. Verify: the grep is clean; `npm run typecheck` passes.
- [x] 4.5 Run the full verification: `npm run typecheck`, `npm test`, and `node_modules/.bin/openspec validate align-verwaltung-offering-configs-to-admin-base`. Verify: all pass.
