## ADDED Requirements

*(No new capabilities — this change is a pure refactoring that replaces manual route definitions with the `resources()` helper. No behavioral changes or new requirements.)*

## MODIFIED Requirements

*(No existing capabilities are modified — all existing routes behave identically before and after the change.)*

## REMOVED Requirements

*(No requirements are removed.)*
