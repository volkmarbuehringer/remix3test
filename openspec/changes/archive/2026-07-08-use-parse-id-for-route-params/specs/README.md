# Use parseId for Route Params

This change has no spec-level impact. It consolidates repeated manual ID parsing across action controllers onto the existing `parseId()` utility. No requirements are added, modified, or removed.
