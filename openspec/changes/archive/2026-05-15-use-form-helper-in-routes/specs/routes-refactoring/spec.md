## No Requirements Change

This change is a pure refactoring of route definitions in `app/routes.ts`. No new capabilities are introduced and no existing capabilities are modified. The external behavior, route structure, and API surface remain identical.

See `proposal.md` for motivation and `design.md` for implementation approach.
