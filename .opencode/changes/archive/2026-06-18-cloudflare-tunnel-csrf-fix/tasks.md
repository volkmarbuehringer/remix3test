# Tasks

- [x] Add `origin: /\.trycloudflare\.com$/` to the `csrf()` call in
  `app/middleware/root.ts`
- [ ] Test login through Cloudflare Tunnel works
- [ ] Verify non-tunnel environments still pass same-origin CSRF checks
