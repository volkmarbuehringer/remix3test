## 1. Configuration Change

- [x] 1.1 Add `"erasableSyntaxOnly": true` to `compilerOptions` in `tsconfig.json`

## 2. Verification

- [x] 2.1 Run `pnpm typecheck` to confirm no new errors
- [x] 2.2 Run `pnpm test` to confirm no test regressions
