## No Requirements Change

This change is a bug fix for the workflow execution engine. No new capabilities are introduced and no existing capabilities are modified. The external behavior is identical — workflows that previously failed silently now execute correctly.

See `proposal.md` for motivation and `design.md` for implementation approach.
