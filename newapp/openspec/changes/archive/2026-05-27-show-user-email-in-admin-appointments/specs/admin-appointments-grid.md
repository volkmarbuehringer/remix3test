## ADDED Requirements

*(No new requirements — this is an implementation-level change to the admin appointments grid that replaces the displayed user name with the user email, without changing any spec-level behavior.)*

## MODIFIED Requirements

*(No existing requirements modified — the grid's functionality (display, sort, search) remains unchanged; only the displayed field changes from name to email.)*
