## No Requirements Change

This change is a visual polish improvement to the dark/light mode switch. No new capabilities are introduced and no existing capabilities are modified. The theme toggle behavior is unchanged.

See `proposal.md` for motivation and `design.md` for implementation approach.
